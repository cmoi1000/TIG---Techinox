# Copiez ce fichier en .env (jamais commité — voir .gitignore) et renseignez
# votre clé API Anthropic. Elle ne sera jamais servie au navigateur : le serveur
# l'utilise uniquement pour proxifier l'extraction de factures (/api/extract-facture).
ANTHROPIC_API_KEY=sk-ant-api03-...
